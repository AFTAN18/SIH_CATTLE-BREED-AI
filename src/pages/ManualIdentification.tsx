import React from 'react';
import ManualIdentification from '@/components/ManualIdentification';

const ManualIdentificationPage: React.FC = () => {
  return <ManualIdentification />;
};

export default ManualIdentificationPage;
